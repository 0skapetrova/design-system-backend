## ✅ Чек-лист: backend готов к интеграции — статус

### 1. База
- [x] Таблица `users` в финальном виде (структура вычищена в P3).
- [x] Таблица `phone_verifications` в финальном виде.
- [ ] Сидер с dev-пользователями  
  → **Нужно сделать** отдельно (минимум 1–2 пользователя с понятными логинами).

### 2. Контракты
- [x] `UserPublic` / `UserInternal` — структура проговорена и зафиксирована в P2.
- [ ] Эндпойнты `POST /auth/login`, `GET /auth/me` задокументированы до конца  
  → Сейчас есть каркас, но нужен чёткий контракт запрос/ответ/ошибки.
- [ ] На будущее: `POST /auth/phone/send-code`, `POST /auth/phone/verify-code`  
  → Можно пока оставить как раздел “Later / Registration”.

### 3. Авторизация
- [ ] Выбран и зафиксирован механизм (cookie-сессия vs httpOnly JWT).  
- [ ] Реализован `POST /auth/login` (под dev-пользователей).  
- [ ] Реализован `GET /auth/me` (берёт юзера по сессии и отдаёт `UserPublic`).

### 4. Инфраструктура
- [ ] CORS под фронт (`origin`, `credentials: true`) — нужно продумать и включить.  
- [ ] `.env` с `BACKEND_URL`, `FRONTEND_URL`, заглушкой под SMS-провайдера.  
- [ ] Базовый формат ошибок (JSON-ответы с `code`, `message`).

---

## 🧩 Предлагаемая следующая ветка по бэку (P4)

**Ветка:** `P4 — dev-login и /me (MVP авторизация)`

Что в неё включить:
1. Описать контракт `POST /auth/login` и `GET /auth/me` (P2-связка, но ветка в P4).
2. Выбрать механизм авторизации (для MVP — можно cookie-сессию).
3. Реализовать dev-сидер пользователей.
4. Поднять простую реализацию `login` и `/me` + базовый CORS/env.

Если хочешь, следующим шагом можем расписать ТЗ именно для ветки  
`P4 - feature/dev-login-and-me`  в 5–7 коротких пунктов.
