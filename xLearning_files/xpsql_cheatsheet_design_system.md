# 🧭 PSQL Шпаргалка (design-system)

## 🎯 1. Подключение и выход

| Команда | Что делает |
|----------|-------------|
| `psql -U user -d db` | вход в конкретную базу |
| `\c dbname` | переключиться на другую базу |
| `\conninfo` | показать текущее подключение |
| `\q` | выйти |
`psql -d design_system_dev -P pager=off -c "\d+ users" > users_schema.txt` Что делает команда:
-P pager=off — полностью отключает посторонние визуальные отображения, цвет и pager.
> users_schema.txt — записывает результат в обычный текстовый файл.



---

## 📋 2. Навигация по структуре

| Команда | Что показывает |
|----------|----------------|
| `\dn` | схемы |
| `\dt` | таблицы |
| `\di` | индексы |
| `\dv` | представления (views) |
| `\df` | функции |
| `\dx` | расширения |
| `\d+ table` | подробное описание таблицы |
| `\d schema.table` | описание с указанием схемы |

---

## 🔍 3. Работа с запросами

| Команда | Что делает |
|----------|-------------|
| `\timing on` | измеряет время выполнения |
| `\x` | формат “по столбцам” |
| `\pset pager off` | отключает листание |
| `\pset format aligned / unaligned` | формат вывода |
| `\watch 2` | повторяет запрос каждые 2 сек |
| `\i file.sql` | выполнить SQL-скрипт |

---

## ⚙️ 4. Диагностика и анализ

| Команда | Что показывает |
|----------|----------------|
| `\dt+` | таблицы с размерами |
| `\di+` | индексы с размерами |
| `\du` | роли и пользователи |
| `\l+` | базы с размерами |
| `\dRp+ table` | политики RLS |
| `EXPLAIN` / `EXPLAIN (ANALYZE)` | план выполнения |
| `\! clear` | очистить экран |
| `\! pwd` | путь в терминале |

---

## 📦 5. Информационные таблицы

| Представление | Что в нём полезно |
|----------------|------------------|
| `information_schema.tables` | список таблиц |
| `information_schema.columns` | структура таблиц |
| `information_schema.table_constraints` | ограничения |
| `pg_indexes` | индексы |
| `pg_constraint` | ограничения |
| `pg_policy` | политики |
| `pg_trigger` | триггеры |
| `pg_extension` | расширения |

---

## 🧠 6. Мини-комбо (на практике)

| Цель | Команда |
|------|----------|
| Проверить таблицу | `\dt projects` |
| Индексы и ограничения | `\d+ projects` |
| Структура колонок | `SELECT column_name, data_type FROM information_schema.columns WHERE table_name='projects';` |
| Последние миграции | `SELECT * FROM schema_migrations ORDER BY run_on DESC LIMIT 5;` |
| Активные подключения | `SELECT * FROM pg_stat_activity WHERE datname='design_system_dev';` |

---

## 🏆 Топ‑10 команд (ежедневный минимум)

1. `\conninfo` — проверить подключение  
2. `\dt` — список таблиц  
3. `\d+ projects` — структура таблицы  
4. `\dx` — список расширений  
5. `\timing on` — включить таймер  
6. `\x` — читаемый формат вывода  
7. `\pset pager off` — отключить постраничный вывод  
8. `EXPLAIN (ANALYZE)` — анализ запроса  
9. `SELECT * FROM schema_migrations LIMIT 5;` — последние миграции  
10. `\q` — выход
